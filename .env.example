# ── Environment config for wardrobe-twin-agent ──
# Copy this to .env and fill in your keys.

# External LLM API keys (leave blank for offline-only mode)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Ollama host (local LLM)
OLLAMA_HOST=http://localhost:11434

# Device: "cuda" or "cpu"
# DEVICE=cpu
